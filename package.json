{
  "name": "channel-clear",
  "version": "0.1.0",
  "description": "Type in the networks your phone already sees, get the least-congested Wi-Fi channel in one click.",
  "keywords": ["wifi", "wireless", "channel", "spectrum", "router", "networking"],
  "type": "module",
  "private": true,
  "license": "MIT",
  "author": "Charlie Krug",
  "repository": {
    "type": "git",
    "url": "https://github.com/ctkrug/channel-clear.git"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@eslint/js": "^9.9.0",
    "eslint": "^9.9.0",
    "jsdom": "^25.0.0",
    "prettier": "^3.3.3",
    "vite": "^5.4.0",
    "vitest": "^2.0.5"
  }
}
